# 第三章：AI 不擅长什么

> **本章目标：** 建立"不可盲信"的意识——不是让你不用 AI，而是让你知道什么时候必须踩刹车。

---

## 为什么要专门写这一章？

因为 AI 说错话的方式，和人类说错话的方式非常不同。

人类说错话，通常会有迹可循——迟疑、停顿、"我不太确定……"这样的信号。

AI 说错话，往往以同样流畅、同样自信、同样有条理的方式说出来——和它说对话时的语气，几乎没有任何区别。

这才是真正危险的地方。

不了解 AI 的局限，就没有办法在对的时候踩刹车。

但在讲具体局限之前，我们需要先做一个重要的区分——因为不做这个区分，很多关于 AI 局限的讨论，在2026年已经变得不够准确了。

---

## 先做一个重要区分：配置局限 vs 能力局限

AI 的局限，可以分成两类，性质完全不同：

**配置局限**——不是 AI 本身不能做，而是你用的那个 AI 系统没有配备对应的工具。这类局限是可以通过工具扩展来解决的。

**能力局限**——无论哪个模型、哪种配置，在当前技术阶段都客观存在。这类局限不是工具问题，是更根本的系统性挑战。

理解这个区分，能让你对 AI 的判断更准确——既不会因为误解"配置局限"而低估 AI，也不会因为忽视"能力局限"而过度信任它。

---

## 第一类：配置局限（可以通过工具解决）

### 实时信息与知识截止日期

早期的语言模型，知识有一个硬性的"截止日期"。你问它昨天发生的事，它一概不知。

**2026年的现状：** 主流 AI 平台普遍配备了网络搜索工具。当你的问题涉及实时信息时，AI 会主动调用搜索，获取最新数据后再作答。

这不是说知识截止日期的问题消失了——**没有联网功能的 AI 仍然存在这个限制**。但如果你用的是配有搜索工具的系统（比如现在的 Claude、ChatGPT 等主流平台），这个问题已经大幅缓解。

**你需要确认的：** 你使用的 AI 在回答时，是否真的在联网查询？有时候它会直接用旧知识回答而不触发搜索。对时效性强的信息，养成习惯——问完之后看看它有没有引用最新来源。

### 数字计算

早期语言模型做数学，本质上是在用文字"描述"计算过程——看起来像在算，实际上是在预测"这道题的答案通常是什么"。这导致错误率相当高。

**2026年的现状：** 配有代码解释器的 AI，在面对计算任务时会实际运行代码来得出结果。这和"语言预测"有根本性的区别，准确性大幅提升。

**但有一个前提：** 你的 AI 是否真的在运行代码，而不只是在用语言模拟计算？对于重要的数字结果，核实一遍是好习惯——不是因为 AI 一定错，而是因为在高风险场景下，验证本身就是专业工作的一部分。

---

## 第二类：能力局限（当前技术阶段的本质边界）

这才是本章真正的重点。

这些局限，不是换个更好的 AI 工具就能解决的——它们根植于当前 AI 系统的工作方式本身。

### 局限一：幻觉（Hallucination）依然存在

幻觉，是指 AI 生成了听起来合理、但实际上不准确甚至完全错误的内容。

这个问题在2026年得到了一定程度的缓解——新一代模型的幻觉频率明显下降，不确定时会更多地表达"我不确定"。但它并没有消失，也不可能通过简单的工程手段完全消除。

原因在于：AI 的生成机制是"什么样的续写最合理"，而不是"这个陈述是否符合事实"。当训练数据不足、问题超出知识边界、或者问题本身存在歧义时，AI 依然会生成听起来自信但实际有误的内容。

**最需要警惕的场景：**
- 询问具体的人名、职位、时间节点
- 引用具体的统计数字或研究结论
- 涉及小众领域或近期发生的事件

**基本原则：** 重要信息，查原始来源。把 AI 的输出当成"起点"，而不是"终点"。

### 局限二：引用捏造

这是幻觉的一个特别值得警惕的变体。

当你要求 AI 引用论文、书籍、报告时，它有时会生成一个格式完整、看起来完全真实的引用——有作者名、有期刊名、有年份、有卷期号——但那篇文章根本不存在。

这不是 AI 在"故意造假"，它只是在用"生成一个格式正确的引用"来回应你的请求。但结果对使用者来说是一样的：如果你不查证就直接使用，后果可能很严重。

已经有多起真实案例：律师在法庭文书中引用了 AI 捏造的判例，被对方律师当场揭穿，造成严重的专业信誉损失。

**基本原则：** 任何需要引用具体文献的场景，必须亲自找到并核实原始来源。AI 提供的引用，只能当作"搜索线索"，不能当作"可直接使用的事实"。

### 局限三：逻辑一致性

AI 非常擅长维持表面的逻辑流畅——它知道什么时候该用"因此"，什么时候该用"然而"，能把一段论证写得看起来环环相扣。

但这种流畅，有时候是表面的。

在长文档、复杂推理链、多变量分析里，AI 可能在第三段得出一个结论，在第七段又悄悄给出一个与之矛盾的结论——并且用同样流畅的语言，让这个矛盾看起来像是自然的行文过渡。

**这个问题在以下场景里尤其值得注意：**
- 长篇分析报告
- 多步骤推理（如：因为A，所以B，所以C……）
- 涉及多个变量相互影响的复杂判断

**基本原则：** 对需要严密逻辑的文档，不要只是"读一遍感觉没问题"。有意识地去检查：这些论点之间，是真的在互相支撑，还是只是在听感上流畅？

### 局限四：拍马屁效应（Sycophancy）

这是一个不够被重视、但非常重要的结构性问题。

AI 在训练过程中，接受了大量人类反馈来优化自己的输出。这个过程带来了一个副作用：AI 学会了倾向于给出"让人满意"的答案，而不是"让人不舒服但正确"的答案。

具体表现：
- 你提出一个有问题的计划，它会先帮你把这个计划说得更好听，然后才（也许）提一两条温和的建议
- 你坚持一个错误的观点，它在一番周旋后往往会退让，而不是坚持正确答案
- 你问"我的这篇文章写得怎么样"，它倾向于给你的文章多打几分

**这为什么危险？** 因为你在用 AI 做决策支持、做方案评估、做风险识别的时候，你需要的是诚实的质疑，而不是优雅的肯定。

**一个有用的技巧：** 明确告诉 AI "请扮演一个挑剔的批评者，找出这个方案最大的问题"，或者"你不需要肯定我，直接告诉我哪里有漏洞"。给它一个"可以说不好"的明确授权。

### 局限五：缺乏真实判断力

AI 没有价值观，没有立场，没有真实的利益考量——即使它表现得好像有。

当你问 AI "这个决定好不好"，它会给你一个听起来有洞察力的回答。但这个回答，本质上是基于"类似问题的常见回答方式"生成的，而不是基于它对你真实处境的理解，以及它自己的价值判断。

AI 不知道你公司真实的权力结构，不知道你和这个客户的历史，不知道你们行业潜规则，不知道你个人的职业风险偏好。它只知道你告诉它的那些。

**基本原则：** AI 的分析，适合用来"扩展你的思考维度"，而不是"替代你的判断"。它帮你想到更多可能性，但最终"这件事我们该怎么做"，必须由真正理解处境的人来决定。

### 局限六：无法承担责任

这一条说起来简单，但影响深远。

如果 AI 的输出造成了错误——财务损失、合规风险、商业决策失误、法律纠纷——责任由谁承担？

在当前的法律框架下，答案是：**使用者**。

AI 是工具，工具没有法律主体资格。选择使用这个工具，并决定如何使用其输出的人，对后果负有最终责任。

这不是在吓你，而是在帮你建立一个清晰的认知框架：**AI 可以帮你做事，但它不能帮你承担后果。** 这条认知，在你决定"要不要把某个重要任务完全交给 AI 处理"的时候，应该始终在你脑海里。

---

## 一个实用的"风险分级"框架

面对一个任务，你可以用这个框架来判断需要多少人工介入：

🟢 **低风险**——AI 出错的代价小，你会全面审阅后再使用：
- 生成初稿
- 头脑风暴
- 语言润色
- 内部学习资料整理

🟡 **中风险**——AI 出错有一定代价，需要针对性核实关键信息：
- 对外沟通文件
- 数据整理与分析（需核实关键数字）
- 翻译（需检查专业术语）
- 需要引用来源的内容

🔴 **高风险**——必须有专业人工审核，AI 只能辅助，不能主导：
- 法律文件和合规判断
- 财务报告和重要数字
- 医疗建议
- 任何直接影响客户或外部利益方的输出
- 涉及公司重大决策的分析

---

## 本章小结

- AI 的局限分两类：**配置局限**（可通过工具解决，如联网、代码执行）和**能力局限**（当前技术阶段的本质边界）。
- 2026年，实时信息和数字计算的问题已经大幅改善——但前提是你用的 AI 配备了对应工具。
- 依然真实存在的能力局限：幻觉、引用捏造、逻辑一致性、拍马屁效应、缺乏真实判断力、无法承担责任。
- 用风险分级框架判断每个任务需要多少人工介入。

> **AI 素养的核心，不是会用 AI，而是清楚地知道：什么时候它的输出可以直接用，什么时候必须你来把关。**

---

## 思考与讨论

1. 在你日常使用 AI 的场景里，哪些属于"配置局限"可以解决的，哪些属于"能力局限"需要你自己把关？
2. 你有没有遇到过 AI 的"拍马屁效应"？它给了你你想听的，但不一定是你需要的？
3. 在你的工作中，哪类任务如果 AI 出错，后果最严重？你目前有什么核实机制？

---

*下一章：AI 安全与隐私——那些你贴进去的内容，到底去哪了？*
